---
title: Insert a line break in the HTML editor
description: When you use the Enter key, the editor creates a paragraph element \(&lt;p&gt;\) tag, which appears as a double space.
locale: en-US
canonical_url: https://www.servicenow.com/docs/r/zurich/platform-administration/c\_InsertALineBreak.html
release: zurich
topic_type: concept
last_updated: "2025-07-31"
reading_time_minutes: 1
breadcrumb: [Formatting functions in HTML field editors, Configure the HTML toolbar, Configure a field editor for the HTML field, Reference, Field administration, Forms, fields, and lists, Configure core features, Administer]
---

# Insert a line break in the HTML editor

When you use the `Enter` key, the editor creates a paragraph element \(`<p>`\) tag, which appears as a double space.

To enter a single-line space, use the `Shift` + `Enter` key combination, which inserts a line break \(`<br>`\) tag.

